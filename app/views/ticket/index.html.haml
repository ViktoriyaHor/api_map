%h1.text-center.m-3 Tickets

- @tickets.each_slice(3).each do |row|
  .row.mt-2
    - row.each do |ticket|
      .col-lg-4.col-12.px-1.text-center
        = link_to ticket.request_number, ticket_show_path(ticket)
